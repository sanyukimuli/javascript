// every time an event happens it triggers a js code behind the scenes
//event
//event handling
//event listening HTML element
        // element.addEventListener('event type', handlerFunction)
        // it listens for an event and responds to the event - knock is event, response is handling, then listens 
        //handlerFunction, when you have listened.. do this 

